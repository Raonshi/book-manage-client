import 'package:flutter/material.dart';

class LentPage extends StatelessWidget {
  const LentPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Lent Page'),
    );
  }
}
